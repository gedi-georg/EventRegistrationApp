@model IEnumerable<EventRegistration.Application.DTOs.EventDto>

@{
    ViewBag.Title = "Ürituste nimekiri";
    var currentDate = DateTime.Now;
    var upcomingEvents = Model.Where(e => e.Date >= currentDate);
    var pastEvents = Model.Where(e => e.Date < currentDate);
}

<div class="container mt-4">
    <div class="row mb-4">
        <div class="col-md-6">
            <div class="p-3 mb-2 bg-primary text-white">
                <h2>Ürituste nimekiri</h2>
            </div>
        </div>
        <div class="col-md-6">
            <img src="~/images/pilt.jpg" class="img-fluid" alt="Ürituste pilt" />
        </div>
    </div>

    <div class="row">
        <div class="col-md-6">
            <div class="card">
                <div class="card-header bg-primary text-white">
                    <h3>Tulevased üritused</h3>
                </div>
                <div class="card-body">
                    <table class="table table-striped">
                        <tbody>
                            @{
                                var rowIndex = 1;
                            }
                            @foreach (var eventItem in upcomingEvents)
                            {
                                <tr>
                                    <td>@rowIndex.</td>
                                    <td>
                                        <a asp-action="Details" asp-route-id="@eventItem.Id">@eventItem.Name</a>
                                    </td>
                                    <td>@eventItem.Date.ToString("dd.MM.yyyy")</td>
                                    <td>@eventItem.Location</td>
                                    <td>@(eventItem.Participants?.Count ?? 0)</td>
                                    <td>
                                        <a asp-controller="Participant" asp-action="Create" asp-route-eventId="@eventItem.Id" class="btn btn-primary mb-2">Lisa osavõtja</a>
                                        <a asp-action="Delete" asp-route-id="@eventItem.Id" class="btn btn-danger btn-sm">
                                            <img src="~/images/remove.svg" class="img-fluid" style="width: 16px; height: 16px;" alt="Kustuta"/>
                                        </a>
                                    </td>
                                </tr>
                                rowIndex++;
                            }
                        </tbody>
                    </table>
                    <a asp-action="Create" class="btn btn-primary">Lisa üritus</a>
                </div>
            </div>
        </div>

        <div class="col-md-6">
            <div class="card">
                <div class="card-header bg-primary text-white">
                    <h3>Toimunud üritused</h3>
                </div>
                <div class="card-body">
                    <table class="table table-striped">
                        <tbody>
                            @{
                                var rowIndex2 = 1;
                            }
                            @foreach (var eventItem in pastEvents)
                            {
                                <tr>
                                    <td>@rowIndex2.</td>
                                    <td>
                                        <a asp-action="Details" asp-route-id="@eventItem.Id">@eventItem.Name</a>
                                    </td>
                                    <td>@eventItem.Date.ToString("dd.MM.yyyy")</td>
                                    <td>@eventItem.Location</td>
                                    <td>@(eventItem.Participants?.Count ?? 0)</td>
                                </tr>
                                rowIndex2++;
                            }
                        </tbody>
                    </table>
                </div>
            </div>
        </div>
    </div>
</div>
